[project]
name = "ai-dev-pre-lesson-d01"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.3",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
pythonpath = ["."]
